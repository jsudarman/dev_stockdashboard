services:
  - type: web
    name: sector-etf-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --preload --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
